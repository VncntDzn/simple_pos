export interface MenuProps {
  id: number;
  name: string;
  price: number;
  preparationTime: number;
  cookingTime: number;
  classification: string;
  isChefRecommendation: number;
  pictureUrl: string;
}
