import { Box } from "@mui/material";

const List = () => {
  return (
    <Box
      height={400}
      sx={{
        border: "3px solid red",
      }}
    >
      List
    </Box>
  );
};

List.propTypes = {};

export default List;
